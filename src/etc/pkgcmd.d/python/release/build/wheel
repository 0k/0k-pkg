#!/bin/bash

build="$1"

mkdir -p "$build"
python setup.py bdist_wheel -d dist &&
    mv "dist/"*"-$pkg_version"*".whl" "$build/" &&
    basename "$build/"*"-$pkg_version"*".whl" | sed "s/-$pkg_version.*//" \
        > "$build/.dist-filename"
